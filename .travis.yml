language: node_js
node_js:
  - "node"
cache: npm
dist: trusty
sudo: required
before_script:
  - npm install -g @angular/cli
  - npm install -g firebase-tools
script:
- ng build --prod
deploy:
  # Deploy master branch
  - provider: script
    script: bash deploy_scripts/master.sh
    on:
      branch: master
  # Deploy job branch
  - provider: script
    script: bash deploy_scripts/job.sh
    on:
      branch: job